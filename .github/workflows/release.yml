name: release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release-plan:
    name: release plan placeholder
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: echo "Release automation is not implemented yet."
