[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "canonicalpath-python"
version = "0.0.0"
description = "Experimental lexical CanonicalPath package for Python"
readme = "README.md"
requires-python = ">=3.10"
